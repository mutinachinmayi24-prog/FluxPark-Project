-r requirements.txt

# Linting / formatting / type checking
ruff
mypy
pylint
flake8

# Security & dead-code analysis
bandit[toml]
# Semgrep has no native Windows build (https://github.com/semgrep/semgrep/issues/1330);
# install it on Linux/macOS/WSL — Windows contributors rely on the CI job instead.
semgrep; platform_system != "Windows"
vulture
pip-licenses

# Modernize Python syntax
pyupgrade

# Testing
pytest
pytest-cov

# Dependency vulnerability audit
pip-audit

# Git hooks
pre-commit
